<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FilmSearch" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimaryDark">#FF2060A0</item>
        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>
        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/explode</item>
        <item name="android:windowExitTransition">@transition/explode</item>
    </style>

    <style name="Theme.FilmSearch" parent="Base.Theme.FilmSearch" />
    <declare-styleable name="CustomRatingView">
        <attr name="progress" format="integer" />
        <attr name="stroke" format="float" />
        <attr name="color1" format="color" />
        <attr name="color2" format="color" />
        <attr name="color3" format="color" />
        <attr name="color4" format="color" />
        <attr name="color_background" format="color" />
        <attr name="default_size" format="dimension" />
        <attr name="animate" format="boolean" />
        <attr name="animation_time" format="integer" />
    </declare-styleable>
</resources>
